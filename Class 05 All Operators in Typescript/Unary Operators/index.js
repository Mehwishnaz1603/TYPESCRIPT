// Unary Operators:
// operation is an operation with only one operand. This operand comes either before or after the operator. 
// Unary operators are more efficient than standard JavaScript function calls. Additionally, unary operators can 
// not be overridden, therefore their functionality is guaranteed. Operator.
//Imagine you have loylity card that you use once (++) and thena a coupon that you use once and throw away (--).
var n = 0;
console.log(n++); // Output is 1
// Example:
var a = 5;
var b = 2;
console.log(a++); // Ouput is 6
console.log(a--); // Output is 1
